server:
  port: 8081

spring:
  application:
    name: capra-auth
  profiles:
    active: dev
  cloud:
    nacos:
      discovery:
        # 服务发现地址
        server-addr: localhost:8848
      config:
        # 配置中心地址
        server-addr: localhost:8848
        # 配置文件格式
        file-extension: yml